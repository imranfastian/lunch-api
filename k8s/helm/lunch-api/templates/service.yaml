apiVersion: v1
kind: Service
metadata:
  name: {{ include "lunch-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lunch-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "lunch-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
