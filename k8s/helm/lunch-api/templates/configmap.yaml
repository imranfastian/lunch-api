apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lunch-api.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lunch-api.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.config.port | quote }}
  GIN_MODE: {{ .Values.config.ginMode | quote }}
